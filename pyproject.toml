[project]
name = "dify-plugin-mediawiki"
version = "0.1.1"
description = "Search and read pages on any MediaWiki site"
readme = "README.md"
requires-python = ">=3.12"

dependencies = [
    "dify_plugin>=0.9.0",
    "requests>=2.32",]

[dependency-groups]
dev = []
